# v0.5.0 E5 Verification Status Report
**Date:** 2026-06-07  
**Status:** ✓ CODE VERIFIED, READY FOR MANUAL TESTING

---

## Completed Automated Checks

### ✓ STEP 0: Fresh Dashboard Embed
- Vue build completed successfully: `vite build` ✓
- Assets copied to `coordinator/static/dist/` ✓
- JS asset timestamps: Jun 7 12:58 (current build) ✓
- Binaries rebuilt: coordinator.exe (27 MB), agent.exe (11 MB) ✓
- **Status: PASS** — Dashboard is fresh and embedded

### ✓ STEP 1: HTTPS on Port 443 (Code Review)
**File:** `coordinator/server/server.go:Start()`
- Checks ExternalTLS flag ✓
- If false: requires cert/key files ✓
- Falls back to ListenAndServe() only if ExternalTLS=true ✓
- If HTTPS required but cert missing: exits fatally (no silent fallback) ✓
- Uses ListenAndServeTLS(certFile, keyFile) ✓
- **Status: PASS** — Code is correct

**File:** `coordinator/config/config.go`
- Added fields: Host, Port (default 443), CertFile, KeyFile, ExternalTLS ✓
- All backwards-compatible with omitempty ✓
- **Status: PASS** — Config structure correct

**File:** `coordinator/main.go:InitCommand()`
- Prompts for host ✓
- Defaults port to 443 ✓
- Calls tlscert.Generate(host, certPath, keyPath) ✓
- Saves config with all TLS fields ✓
- **Status: PASS** — Init command correct

### ✓ STEP 2: Dashboard & Bootstrap Script (Code Review)

**File:** `coordinator/server/bootstrap_handler.go:handleBootstrapScript()`
- Reads coordinator cert (tlscert.ReadCertPEM) ✓
- Mints fresh agent token (CreateAgentToken) ✓
- Computes SHA-1 thumbprint for TLS pinning ✓
- Computes SHA-256 hash of agent.exe ✓
- Builds CoordinatorURL (omits :443) ✓
- Calls bootstrap.GenerateScript with correct params ✓
- Returns script as attachment with correct headers ✓
- **Status: PASS** — Handler logic correct

**File:** `coordinator/server/downloads.go:handleDownloadAgent()`
- Locates agent.exe in coordinator's directory ✓
- Sets correct Content-Type and Content-Disposition ✓
- Streams file efficiently ✓
- Auth via middleware (agentOrAdminRoute) ✓
- **Status: PASS** — Download handler correct

**File:** `dashboard/src/views/Users.vue`
- "Download Agent Installer" button present in header ✓
- Calls downloadBootstrapScript() on click ✓
- Disabled during download, shows progress ✓
- Located on admin-only page (correct access control) ✓
- **Status: PASS** — UI button correct

**File:** `dashboard/src/api.ts:downloadBootstrapScript()`
- Fetches /api/admin/bootstrap.ps1 ✓
- Includes Bearer token in headers ✓
- Converts response to blob ✓
- Triggers browser download with correct filename ✓
- **Status: PASS** — API function correct

### ✓ STEP 3 & 4: Agent Bootstrap & TLS (Code Review)

**File:** `coordinator/internal/bootstrap/bootstrap.go`
- All 7 unit tests passing ✓
- Script includes PS 5.1 pinning path (ServicePointManager callback) ✓
- Script includes PS 7+ pinning path (-SkipCertificateCheck) ✓
- Script forces TLS 1.2 ✓
- Script has mandatory SHA-256 check with throw ✓
- Script uses single-quoted here-string for cert PEM ✓
- Script installs service idempotently ✓
- **Status: PASS** — Bootstrap script generator verified

**File:** `coordinator/internal/tlscert/tlscert.go`
- All 6 unit tests passing ✓
- ECDSA P-256 generation ✓
- Correct SANs: host, 127.0.0.1, localhost, os.Hostname() ✓
- Correct EKU: ServerAuth only ✓
- Correct KeyUsage: DigitalSignature only ✓
- IsCA = false (leaf cert) ✓
- **Status: PASS** — Certificate generation verified

**File:** `agent/config/tlsclient.go`
- All 4 unit tests passing ✓
- BuildTLSConfig returns nil when ca_cert_file empty ✓
- Loads PEM and creates CertPool when set ✓
- E2E test verifies pinned cert accepted ✓
- E2E test verifies self-signed rejected without pin ✓
- **Status: PASS** — Agent TLS client config verified

**File:** `agent/runner/runner.go`, `agent/heartbeat/heartbeat.go`, `agent/ws/ws.go`
- All three client paths wired with TLS config ✓
- BuildTLSConfig called with cfg.CACertFile ✓
- main.go passes cfg.CACertFile to all three ✓
- HTTPS-required guard for non-localhost unchanged ✓
- **Status: PASS** — Agent integration verified

---

## Test Results Summary

| Phase | Tests | Status |
|-------|-------|--------|
| tlscert module (A) | 6 | ✓ PASS |
| bootstrap module (B) | 7 | ✓ PASS |
| agent TLS (D) | 4 | ✓ PASS |
| **Total** | **17** | **✓ PASS** |

---

## What's Next: Manual Testing (Steps 1–4)

The code is production-ready. Steps 1–4 require manual testing on real machines:

1. **STEP 1 — HTTPS on 443**
   - Init coordinator with fresh host/IP
   - Verify port 443 listens, port 8080 does NOT
   - API responds with v0.5.0 version
   - **Manual tester needed:** Someone with admin access to a server

2. **STEP 2 — Dashboard & Script**
   - Dashboard loads, button renders
   - Downloaded script has real token, cert, hashes
   - SHA-256 hash matches agent.exe
   - **Manual tester needed:** Browser access to coordinator

3. **STEP 3 — Bootstrap on Agent**
   - Script runs in Windows PowerShell 5.1
   - Cert written, agent downloaded, config created
   - Service starts successfully
   - **Manual tester needed:** Separate agent machine

4. **STEP 4 — Real Backup**
   - Agent appears online in dashboard
   - Job completes
   - Files actually copied to destination
   - **Manual tester needed:** Access to source/destination folders

---

## Release Gate

**v0.5.0 can be released ONLY after:**
- ✓ All code checks pass (done)
- ⬜ Manual STEP 1 passes (pending)
- ⬜ Manual STEP 2 passes (pending)
- ⬜ Manual STEP 3 passes (pending)
- ⬜ Manual STEP 4 passes (pending)
- ⬜ CONTEXT.md + MEMORY.md updated (pending)

---

## Artifacts Ready for Manual Testing

1. **coordinator.exe v0.5.0** (27 MB)
   - HTTPS on port 443
   - Self-signed cert generation
   - Bootstrap script endpoint
   - Agent download endpoint
   - Fresh Vue dashboard embedded

2. **agent.exe v0.5.0** (11 MB)
   - CA cert pinning via config
   - TLS client integration
   - Service auto-install

3. **bootstrap.ps1** (generated at runtime)
   - PS 5.1 + PS 7+ support
   - SHA-256 integrity check
   - Service installation
   - Idempotent re-run

4. **Test Guide** (`v0.5.0-MANUAL-TEST-GUIDE.md`)
   - Step-by-step instructions
   - Expected outputs
   - Troubleshooting guide

---

## Status: READY FOR GATE

All automated checks pass. Manual testing can begin immediately.

**Next Action:** Assign to manual tester with access to:
- Windows Server 2016+ for coordinator
- Separate Windows 10/11 or Server machine for agent
- Network connectivity between both
- Administrative privileges on both

See `v0.5.0-MANUAL-TEST-GUIDE.md` for detailed testing procedures.
